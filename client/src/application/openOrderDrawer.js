/**
 * Tek drawer açılış API — Spec 3.
 * React bileşenleri `useOrderDrawer().openOrderDrawer` kullanır; kuyruk sayfaları `buildDrawerQueue` ile bağlam geçer.
 */
export { buildDrawerQueue, normalizeQueueContext } from './orderDrawerOrchestration.js'
