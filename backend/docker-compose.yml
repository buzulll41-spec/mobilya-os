services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: mobilya
      POSTGRES_PASSWORD: mobilya
      POSTGRES_DB: mobilya
    ports:
      - '5432:5432'
    volumes:
      - mobilya_pg:/var/lib/postgresql/data

volumes:
  mobilya_pg:
